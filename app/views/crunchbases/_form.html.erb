<%= form_for(@crunchbasis) do |f| %>
  <% if @crunchbasis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crunchbasis.errors.count, "error") %> prohibited this crunchbasis from being saved:</h2>

      <ul>
      <% @crunchbasis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
