<h1>Listing organizations</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organizations.each do |organization| %>
        <%= link_to organization["name"], "#{organization['path']}"  %>
      <hr>
    <% end %>
  </tbody>
</table>
